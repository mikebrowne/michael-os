# Objective
add src/utils/greet.ts with greet(name: string) returning "Hello, {name}!"

# Decisions
- Empty or missing name returns "Hello, !"
- Vitest, ESM imports, no new dependencies

# Open questions resolved

# Out of scope
CLI, API, unrelated files
