# Welcome to the Demo Vault

This is a **fake**, public-safe note used by MichaelOS tests and local demos.

The real Vault lives outside the repository and is referenced only through `VAULT_PATH` in `.env`.

## Sample idea

MichaelOS should help the operator finish more projects by building thin vertical slices first.
